# vNAS Sweatbox Scenario Generator Requirements

# Core dependencies
requests>=2.31.0
python-ulid>=2.0.0
packaging>=23.0  # For version comparison in auto-updater
FlightRadarAPI>=1.3.0  # For fetching airport elevation data
beautifulsoup4>=4.12.0  # Required by FlightRadarAPI
lxml>=4.9.0  # HTML/XML parser for BeautifulSoup

# Browser automation (for vNAS integration)
undetected-chromedriver>=3.5.0
selenium>=4.0.0  # Required by undetected-chromedriver and vNAS client
setuptools>=70.0.0  # Required for undetected-chromedriver on Python 3.13+

# Optional: for better performance with large JSON files
# ujson>=5.8.0
